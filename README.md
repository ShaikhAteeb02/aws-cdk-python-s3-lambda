# aws-cdk-python-s3-lambda
AWS CDK project in Python that provisions an S3 bucket with versioning enabled and a Lambda function triggered by S3 object-created events. The stack manages IAM roles for secure permissions, allowing automated infrastructure deployment and event-driven Lambda execution with AWS best practices.
